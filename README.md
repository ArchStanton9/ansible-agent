# ansible-agent

Creates an `ansible` user with SSH access and passwordless sudo for my VPS bootstrap.

Run as `root`:

```sh
curl -fsSL "https://raw.githubusercontent.com/ArchStanton9/ansible-agent/1.0.3/bootstrap-ansible-user.sh" | sh
```
